services:
  - type: worker
    name: shop-alert-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python checker.py
    envVars:
      - key: DISCORD_WEBHOOK_URL
        sync: false
